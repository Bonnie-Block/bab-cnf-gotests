apiVersion: ran.openshift.io/v1alpha1
kind: ClusterGroupUpgrade
metadata:
  name: talm-spoke-test-with-missing-spoke
  namespace: default
spec:
  clusters:
   - non-existent-cluster
  managedPolicies:
   - non-existent-policy
  remediationStrategy:
    maxConcurrency: 99
enable: true
