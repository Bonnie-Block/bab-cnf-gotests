package tests

import (
	. "github.com/onsi/ginkgo/v2"

	"gitlab.cee.redhat.com/cnf/cnf-gotests/test/util/execute"
)

var _ = Describe("Talm Precaching Tests", func() {

	execute.BeforeAll(func() {
	})

	BeforeEach(func() {
	})

	Describe("Precaching feature enabled", func() {
		// Context("where it fails for one spoke", func() {
		// 	It("errors the CGU", func() {
		// 		// Polarion test id tbd
		// 		// https://issues.redhat.com/browse/CNF-6499
		// 	})
		// })
	})
})
